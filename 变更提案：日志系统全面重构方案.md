 这是一个典型的**环境感知型日志架构**问题。核心思路是：让日志系统"聪明地"知道自己在什么环境，从而自动调整输出策略。

以下是完整的改造规划方案：

---

## 一、核心设计原则

| 环境 | 目标 | 策略 |
|------|------|------|
| **开发/调试** | 快速定位问题 | 全量输出、详细上下文、堆栈追踪 |
| **测试/预发** | 验证功能稳定性 | 结构化日志、保留关键链路 |
| **生产/交付** | 用户交互体验 | 极简流程提示、错误收敛、性能无关日志静默 |

---

## 二、日志级别重新规划

不要只用 `info`，建议建立**五级+体系**：

```
DEBUG   → 开发专用：变量值、循环迭代、SQL语句、API请求/响应体
INFO    → 运行里程碑：服务启动/停止、配置加载完成、核心流程节点
WARN    → 可恢复异常：请求重试、降级处理、非关键依赖超时
ERROR   → 功能受损：业务逻辑失败、数据异常、必要资源不可用
FATAL   → 系统崩溃：无法启动、核心组件初始化失败
```

**关键改造点**：把原先混在 `INFO` 里的"调试信息"下沉到 `DEBUG`，把"错误提示"上浮到 `ERROR`。

---

## 三、环境感知配置架构

这是实现"开发详细、生产精简"的核心机制。

### 1. 多环境配置文件
```yaml
# log-config.dev.yaml  (开发环境)
level: DEBUG
format: text          # 人类可读，带颜色
outputs: [console, file]
file:
  path: ./logs/app.log
  max_size: 100MB
  retention: 7d
include:
  - timestamp: true
  - caller: true        # 文件名:行号
  - stack_trace: true   # 错误时打印堆栈
  - request_id: true
  - user_id: true
  - execution_time: true
```

```yaml
# log-config.prod.yaml  (生产环境)
level: INFO
format: json            # 结构化，便于收集分析
outputs: [file]         # 不输出到控制台，避免污染用户界面
file:
  path: /var/log/app/app.log
  max_size: 500MB
  retention: 30d
include:
  - timestamp: true
  - caller: false       # 隐藏技术细节
  - stack_trace: false  # 堆栈写入文件，不暴露给用户
  - request_id: true
  - user_id: false      # 隐私合规
  - execution_time: false
```

### 2. 用户可见层 vs 系统日志层（重点）

**建议采用双通道架构**：

| 通道 | 目标 | 内容 |
|------|------|------|
| **用户界面日志** | 终端/前端展示 | 极简中文提示，如"正在连接服务器..." "同步完成" |
| **系统诊断日志** | 文件/远程收集 | 完整技术细节，用于问题排查 |

```python
# 伪代码示例
class AppLogger:
    def __init__(self):
        self.system_logger = get_logger("system")   # 全量技术日志
        self.user_logger = get_logger("user")       # 用户可见日志
    
    def sync_data(self):
        self.user_logger.info("正在同步数据，请稍候...")      # 用户看到
        self.system_logger.debug("Sync job started, params: {...}")  # 调试看到
        
        try:
            result = api.call()
            self.system_logger.debug(f"API response: {result}")
            self.user_logger.info("数据同步成功")              # 用户看到
        except Exception as e:
            self.system_logger.error("Sync failed", exc_info=True)   # 详细错误
            self.user_logger.warn("同步遇到问题，已启用离线模式")    # 友好提示
```

---

## 四、日志内容规范（按场景）

### 开发环境应包含的字段
- `timestamp` (精确到毫秒)
- `level` (级别)
- `logger_name` (模块名)
- `file:line` (调用位置)
- `function` (函数名)
- `request_id` (请求链路ID，用于追踪单次操作全链路)
- `user_id` / `session_id`
- `elapsed_ms` (执行耗时)
- `context` (关键变量快照)

### 生产环境应精简为
- `timestamp`
- `level`
- `message` (用户友好的中文提示)
- `request_id` (仅保留，用于后端关联排查)

---

## 五、关键技术实现建议

### 1. 动态日志级别调整（热切换）
生产环境发现问题时，无需重启即可临时开启 DEBUG：
```bash
# 通过信号或管理接口
curl -X POST /admin/log-level -d '{"module":"payment","level":"DEBUG","duration":"10m"}'
```

### 2. 采样日志（高频率操作）
对于高频循环或定时任务，生产环境避免日志爆炸：
```python
# 每100次只记录1次 INFO，但 ERROR 全量记录
logger.info("Heartbeat check", extra={"sample_rate": 0.01})
```

### 3. 错误脱敏与收敛
```python
# 生产环境：同一类错误只在前端提示一次，避免刷屏
if error_type not in shown_errors:
    user_logger.error("网络连接异常，请检查设置")
    shown_errors.add(error_type)

# 系统日志仍全量记录
system_logger.error(f"Connection timeout: {details}")
```

### 4. 日志轮转与清理
- 开发环境：按天轮转，保留7天
- 生产环境：按大小轮转（500MB），保留30天，同时考虑远程投递（如 ELK / Loki）

---

## 六、实施路线图

建议分阶段改造，避免一次性重构引入新问题：

| 阶段 | 动作 | 周期 |
|------|------|------|
| **第1周** | 统一日志库选型（推荐 `loguru`(Python)、`pino`(Node)、`logback`(Java) 等），替换所有 `print` 和零散日志 | 短期 |
| **第2周** | 梳理现有 `INFO` 日志，按规范分级为 `DEBUG/INFO/WARN/ERROR` | 短期 |
| **第3周** | 引入环境配置，实现开发/生产自动切换输出策略 | 中期 |
| **第4周** | 分离用户可见日志和系统诊断日志双通道 | 中期 |
| **持续** | 补充关键路径的 `request_id` 链路追踪，完善上下文信息 | 长期 |

---

## 七、快速检查清单

改造完成后，用以下场景验证：

- [ ] 开发环境启动时，能看到配置加载详情、数据库连接参数、路由注册信息（DEBUG）
- [ ] 开发环境报错时，能看到完整堆栈和变量上下文
- [ ] 生产环境启动时，用户只看到"服务启动成功"（INFO）
- [ ] 生产环境报错时，用户看到友好提示，但日志文件里有完整错误详情
- [ ] 能通过一个 `request_id` 从用户操作追溯到系统全链路日志