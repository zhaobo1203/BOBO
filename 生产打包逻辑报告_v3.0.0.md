# 生产打包逻辑报告 v3.0.0

## 文档信息

| 项目 | 内容 |
|------|------|
| 项目名称 | 微信群消息监听与股票分析系统 |
| 版本号 | v3.0.0 |
| 报告日期 | 2026-07-25 |
| 报告类型 | 生产打包逻辑规范 |
| 适用范围 | 模块1（微信监控）+ 模块2（A股数据管理）+ 模块3（股票分析）统一打包 |

---

## 1. 打包决策总览

### 1.1 核心决策

| 决策项 | 选择 | 说明 |
|--------|------|------|
| 打包范围 | 模块1+2+3整体打包 | 统一为一个EXE |
| 启动方式 | 同时启动微信监控+股票分析 | 用户直接进入选群聊交互 |
| 打包模式 | onefile（单文件） | 分发简单，一个EXE即可 |
| 入口文件 | 新建 `src/main.py` | 不影响原有模块代码 |
| 数据文件策略 | 与EXE同目录 | 方便用户查看和管理 |
| A股数据库 | 打包初始库到EXE，首次释放 | 后续可通过API手动更新 |
| 隐式导入 | A+B混合策略 | 模块1手动列出 + 新增包自动收集 |
| 排除模块 | 沿用模块1排除列表 | 后续按需调整 |
| 发布包 | EXE + vc_redist.x64.exe + 使用说明.txt | 最小化发布 |
| 版本号命名 | 微信群消息监听_v3.0.0.exe | 三模块统一版本 |

### 1.2 与模块1打包的差异对比

| 对比项 | 模块1（v1.0/v2.0.0） | 统一版（v3.0.0） |
|--------|----------------------|------------------|
| 入口文件 | `src/simple_monitor.py` | `src/main.py`（新建） |
| 包含模块 | wechat_decrypt_tool | wechat_decrypt_tool + a_stock_db + stock_analysis |
| 隐式导入 | 全部手动列出 | 手动列出 + 自动收集混合 |
| 数据文件 | Native DLL | Native DLL + A股数据库 + blacklist.json |
| 运行时数据 | output/、logs/ | output/、logs/、data/ |
| 服务进程 | 仅微信监控 | 微信监控 + FastAPI服务 |
| spec文件 | simple_monitor.spec | release_v3.spec（新建） |
| 构建脚本 | build_exe.bat | build_exe_v3.bat（新建） |

---

## 2. 项目模块架构

### 2.1 模块结构

```
src/
├── main.py                          # 【新建】统一入口文件
├── simple_monitor.py                # 模块1：微信监控（原有，不修改）
├── wechat_decrypt_tool/             # 模块1：微信解密工具包
│   ├── native/                      # Native DLL文件
│   │   ├── wcdb_api.dll             # WCDB数据库接口
│   │   ├── WCDB.dll                 # 微信数据库引擎
│   │   ├── VoipEngine.dll           # 语音通话引擎
│   │   ├── wce_integrity.pyd        # 完整性校验模块
│   │   ├── img_helper.dll           # 图片处理
│   │   ├── SDL2.dll                 # SDL运行时
│   │   ├── msvcp140.dll / msvcp140_1.dll
│   │   ├── vcruntime140.dll / vcruntime140_1.dll
│   │   └── weflow_wasm/             # WASM密钥流处理
│   │       ├── weflow_wasm_keystream.js
│   │       ├── wasm_video_decode.js
│   │       └── wasm_video_decode.wasm
│   └── ...（其他Python模块）
├── a_stock_db/                      # 模块2：A股数据管理
│   ├── __init__.py
│   ├── data_sources.py              # 数据源管理（Baostock/AKShare/Efinance等）
│   ├── database.py                  # SQLite数据库操作
│   └── main.py                      # 模块2入口
└── stock_analysis/                  # 模块3：股票分析服务
    ├── __init__.py
    ├── main.py                      # FastAPI应用入口
    ├── dashboard.py                 # 终端看板
    ├── config/
    │   ├── settings.py              # 配置文件
    │   └── blacklist.json           # 股票名黑名单
    ├── api/
    │   └── routes.py                # API路由
    ├── models/
    │   ├── mention.py               # 提及记录模型
    │   └── stock.py                 # 股票模型
    └── services/
        ├── matcher.py               # 股票名匹配引擎
        ├── statistics.py            # 统计服务
        ├── stock_loader.py          # A股数据加载器
        └── storage.py               # 存储服务
```

### 2.2 运行时数据目录

```
EXE所在目录/
├── 微信群消息监听_v3.0.0.exe
├── data/                            # 运行时数据目录
│   ├── a_stock_db/
│   │   └── a_stock.db               # A股数据库（首次从EXE释放）
│   ├── messages.db                  # 消息数据库（运行时创建）
│   └── stock_mentions.db            # 匹配结果数据库（运行时创建）
├── logs/                            # 日志目录
│   ├── app_YYYY-MM-DD.log           # 应用日志
│   └── stock_analysis_YYYY-MM-DD.log # 股票分析日志
├── output/                          # 输出目录
│   └── account_keys.json            # 密钥存储
└── vc_redist.x64.exe                # VC++运行时安装包
```

---

## 3. 统一入口文件设计

### 3.1 `src/main.py` 核心逻辑

```python
#!/usr/bin/env python3
"""
微信群消息监听与股票分析系统 - 统一入口 v3.0.0
同时启动微信监控（模块1）和股票分析服务（模块3）
"""

import sys
import os
import threading
import multiprocessing
from pathlib import Path

# Windows PyInstaller 打包必须
multiprocessing.freeze_support()

# 添加项目路径
if not getattr(sys, 'frozen', False):
    sys.path.insert(0, str(Path(__file__).parent))

# 获取EXE所在目录（运行时数据根目录）
def get_app_dir() -> Path:
    if getattr(sys, 'frozen', False):
        return Path(sys.executable).parent
    else:
        return Path(__file__).resolve().parents[1]

APP_DIR = get_app_dir()
```

### 3.2 启动流程

```
程序启动
  │
  ├── 1. multiprocessing.freeze_support()
  ├── 2. 确定应用根目录（EXE同目录）
  ├── 3. 释放A股数据库（首次运行检测）
  │     └── 检查 data/a_stock_db/a_stock.db 是否存在
  │         └── 不存在 → 从EXE内部资源释放
  ├── 4. 设置运行时路径（覆盖settings.py中的路径配置）
  │     ├── A_STOCK_DB_PATH → APP_DIR/data/a_stock_db/a_stock.db
  │     ├── MESSAGES_DB_PATH → APP_DIR/data/messages.db
  │     ├── STOCK_MENTIONS_DB_PATH → APP_DIR/data/stock_mentions.db
  │     ├── LOG_DIR → APP_DIR/logs
  │     └── BLACKLIST_PATH → 从EXE内部读取
  ├── 5. 后台线程启动 FastAPI 服务（模块3）
  │     └── uvicorn.run(stock_analysis.main:app, host=0.0.0.0, port=8000)
  ├── 6. 主线程运行微信监控（模块1）
  │     └── SimpleMonitor().run()
  │         ├── 进程检测 → 账号识别 → 密钥获取
  │         ├── 数据库连接 → 选择群聊 → 实时监控
  │         └── 监控消息写入 messages.db（模块3自动增量匹配）
  └── 7. Ctrl+C 优雅退出
        ├── 停止 FastAPI 服务
        └── 停止微信监控
```

### 3.3 关键设计要点

#### 3.3.1 运行时路径重定向

模块3的 `settings.py` 使用 `Path(__file__).resolve().parents[3]` 作为项目根目录，在打包后此路径指向临时解压目录，需要重定向到EXE同目录。

**解决方案**：在 `src/main.py` 中，启动FastAPI之前，通过修改模块级变量覆盖settings中的路径：

```python
def redirect_settings_paths():
    """重定向模块3的配置路径到EXE同目录"""
    from stock_analysis.config import settings
    settings.PROJECT_ROOT = APP_DIR
    settings.A_STOCK_DB_PATH = APP_DIR / "data" / "a_stock_db" / "a_stock.db"
    settings.MESSAGES_DB_PATH = APP_DIR / "data" / "messages.db"
    settings.STOCK_MENTIONS_DB_PATH = APP_DIR / "data" / "stock_mentions.db"
    settings.LOG_DIR = APP_DIR / "logs"
```

同样，模块2的 `database.py` 使用 `Path(__file__).parent.parent.parent / "data" / "a_stock_db"` 作为默认路径，也需要重定向：

```python
def redirect_a_stock_db_paths():
    """重定向模块2的数据库路径到EXE同目录"""
    from a_stock_db import database
    database.DEFAULT_DB_DIR = APP_DIR / "data" / "a_stock_db"
    database.DEFAULT_DB_PATH = database.DEFAULT_DB_DIR / "a_stock.db"
```

#### 3.3.2 A股数据库首次释放

onefile模式下，打包在EXE内的文件运行时解压到临时目录（`sys._MEIPASS`）。首次运行时需要将A股数据库复制到EXE同目录：

```python
def ensure_a_stock_db():
    """确保A股数据库存在于EXE同目录"""
    target_path = APP_DIR / "data" / "a_stock_db" / "a_stock.db"
    if target_path.exists():
        return  # 已存在，无需释放
    if getattr(sys, 'frozen', False):
        source_path = Path(sys._MEIPASS) / "data" / "a_stock_db" / "a_stock.db"
        if source_path.exists():
            target_path.parent.mkdir(parents=True, exist_ok=True)
            import shutil
            shutil.copy2(str(source_path), str(target_path))
            print(f"  已释放A股数据库到: {target_path}")
```

#### 3.3.3 FastAPI服务后台启动

FastAPI服务在后台线程中启动，不阻塞主线程的微信监控交互：

```python
def start_fastapi_server():
    """在后台线程启动FastAPI服务"""
    import uvicorn
    redirect_settings_paths()
    redirect_a_stock_db_paths()
    (APP_DIR / "data").mkdir(exist_ok=True)
    (APP_DIR / "logs").mkdir(exist_ok=True)
    def run_server():
        uvicorn.run(
            "stock_analysis.main:app",
            host="0.0.0.0", port=8000, log_level="info",
        )
    server_thread = threading.Thread(target=run_server, daemon=True)
    server_thread.start()
    return server_thread
```

#### 3.3.4 Blacklist.json 读取

`blacklist.json` 打包在EXE内部，需要从 `sys._MEIPASS` 读取：

```python
def get_blacklist_path() -> Path:
    """获取blacklist.json路径"""
    if getattr(sys, 'frozen', False):
        return Path(sys._MEIPASS) / "stock_analysis" / "config" / "blacklist.json"
    else:
        return Path(__file__).parent / "stock_analysis" / "config" / "blacklist.json"
```

需要在重定向settings时一并设置：

```python
settings.BLACKLIST_PATH = get_blacklist_path()
```

---

## 4. PyInstaller 打包配置

### 4.1 spec文件：`release_v3.spec`

#### 4.1.1 数据文件收集

| 类别 | 源路径 | 目标路径 | 说明 |
|------|--------|----------|------|
| Native DLL | `src/wechat_decrypt_tool/native/*.dll` | `wechat_decrypt_tool/native` | 模块1必需 |
| Native PYD | `src/wechat_decrypt_tool/native/*.pyd` | `wechat_decrypt_tool/native` | 模块1必需 |
| WASM/JS | `src/wechat_decrypt_tool/native/weflow_wasm/*` | `wechat_decrypt_tool/native/weflow_wasm` | 模块1必需 |
| A股数据库 | `data/a_stock_db/a_stock.db` | `data/a_stock_db` | 首次释放用 |
| 黑名单配置 | `src/stock_analysis/config/blacklist.json` | `stock_analysis/config` | 模块3必需 |

#### 4.1.2 隐式导入策略

**模块1（手动列出，已验证）：**

```
# 标准库
sqlite3, winreg, ctypes, ctypes.wintypes,
multiprocessing, multiprocessing.freeze_support,
concurrent, concurrent.futures, concurrent.futures.thread,
threading, queue, socket, hashlib, hmac, struct, json, re,
logging, logging.handlers, datetime, pathlib, tempfile, shutil,
typing, dataclasses, collections, functools, itertools, warnings

# 第三方库
psutil, psutil._pswindows, psutil._common
pymem, pymem.exception, pymem.memory, pymem.process, pymem.ressources
Crypto, Crypto.Cipher, Crypto.Cipher.AES, Crypto.Protocol.KDF,
Crypto.Hash, Crypto.Random, Crypto.Util, Crypto.Util.Padding
cryptography, cryptography.hazmat, cryptography.hazmat.backends,
cryptography.hazmat.primitives, cryptography.hazmat.primitives.ciphers
zstandard, wx_key

# 内部模块 - wechat_decrypt_tool（完整列表见simple_monitor.spec）
wechat_decrypt_tool, wechat_decrypt_tool.constants, ...
```

**模块2/3新增（自动收集）：**

```python
from PyInstaller.utils.hooks import collect_submodules

# FastAPI + uvicorn（大型包，自动收集子模块）
hiddenimports_m3_auto = collect_submodules('fastapi')
hiddenimports_m3_auto += collect_submodules('uvicorn')
hiddenimports_m3_auto += collect_submodules('uvicorn.lifespan')
hiddenimports_m3_auto += collect_submodules('uvicorn.protocols')
hiddenimports_m3_auto += collect_submodules('uvicorn.logging')

# A股数据源（自动收集子模块）
hiddenimports_m2_auto = collect_submodules('akshare')
hiddenimports_m2_auto += collect_submodules('baostock')
hiddenimports_m2_auto += collect_submodules('efinance')
```

**模块2/3内部模块（手动列出）：**

```
# 模块2 - a_stock_db
a_stock_db, a_stock_db.data_sources, a_stock_db.database

# 模块3 - stock_analysis
stock_analysis, stock_analysis.main, stock_analysis.dashboard,
stock_analysis.config, stock_analysis.config.settings,
stock_analysis.api, stock_analysis.api.routes,
stock_analysis.models, stock_analysis.models.mention, stock_analysis.models.stock,
stock_analysis.services, stock_analysis.services.matcher,
stock_analysis.services.statistics, stock_analysis.services.stock_loader,
stock_analysis.services.storage
```

**模块3新增第三方库（手动列出）：**

```
# 中文分词
jieba

# 拼音索引
pypinyin

# 日志
loguru

# HTTP客户端
httpx, httpx._transports, httpx._transports.default

# ASGI
anyio, anyio._backends
starlette, starlette.routing, starlette.middleware
```

#### 4.1.3 排除模块

沿用模块1排除列表：

```python
excludes = [
    'tkinter', 'matplotlib', 'numpy', 'pandas', 'PIL',
    'cv2', 'torch', 'tensorflow', 'scipy',
    'IPython', 'jupyter', 'pytest', 'sphinx',
]
```

> **注意**：akshare间接依赖pandas，但运行时需要pandas处理DataFrame。如果打包后akshare功能异常，可能需要将pandas从排除列表中移除。

#### 4.1.4 完整spec文件结构

```python
# release_v3.spec 核心结构
a = Analysis(
    ['src/main.py'],                          # 统一入口
    pathex=[PROJECT_ROOT, os.path.join(PROJECT_ROOT, 'src')],
    binaries=[],
    datas=filtered_datas,                      # 收集的数据文件
    hiddenimports=hiddenimports_m1 + hiddenimports_m2_auto + hiddenimports_m3_auto + hiddenimports_m2_m3_manual,
    hookspath=[],
    excludes=[...],                            # 排除模块
    noarchive=False,
    optimize=1,
)

pyz = PYZ(a.pure, a.zipped_data)

exe = EXE(
    pyz, a.scripts, a.binaries, a.zipfiles, a.datas, [],
    name='微信群消息监听_v3.0.0',
    debug=False,
    console=True,
    upx=False,                                 # 禁用UPX，保证兼容性
    uac_admin=False,                           # 不强制管理员权限
)
```

---

## 5. 构建脚本

### 5.1 `build_exe_v3.bat` 流程

```
build_exe_v3.bat
  │
  ├── [1/7] 检查环境
  │     ├── Python 3.11+ 是否可用
  │     ├── PyInstaller 是否已安装
  │     └── release_v3.spec 是否存在
  │
  ├── [2/7] 清理旧构建
  │     ├── 删除 build/ 目录
  │     └── 删除 dist/ 目录
  │
  ├── [3/7] 检查A股数据库
  │     └── 确认 data/a_stock_db/a_stock.db 存在
  │
  ├── [4/7] 执行PyInstaller打包
  │     └── python -m PyInstaller release_v3.spec --noconfirm
  │
  ├── [5/7] 验证输出
  │     ├── 检查 dist/微信群消息监听_v3.0.0.exe 是否存在
  │     └── 显示文件大小
  │
  ├── [6/7] 准备发布文件
  │     ├── 复制EXE到release/
  │     ├── 复制vc_redist.x64.exe到release/
  │     └── 生成使用说明.txt
  │
  └── [7/7] 完成
        └── 显示打包结果摘要
```

---

## 6. 发布包内容

### 6.1 发布文件清单

| 文件 | 用途 | 说明 |
|------|------|------|
| 微信群消息监听_v3.0.0.exe | 主程序 | 包含模块1+2+3 |
| vc_redist.x64.exe | VC++运行时 | 首次运行前安装 |
| 使用说明.txt | 操作指南 | 包含三模块使用说明 |

### 6.2 使用说明.txt 内容要点

```
微信群消息监听与股票分析系统 v3.0.0

【功能说明】
1. 微信群消息实时监控（模块1）
2. A股数据库管理，支持手动更新（模块2）
3. 股票提及自动匹配与统计分析（模块3）

【使用方法】
1. 确保已安装微信并登录
2. 首次运行请先安装 vc_redist.x64.exe
3. 双击运行 微信群消息监听_v3.0.0.exe
4. 按照提示完成：进程检测→密钥获取→选择群聊
5. 监控启动后，股票分析服务自动运行

【API服务】
- 地址: http://localhost:8000
- 健康检查: GET /api/health
- 手动刷新: POST /api/refresh
- 增量刷新: POST /api/incremental-refresh
- 更新A股数据库: POST /api/update-stock-db
- 日统计: GET /api/stats/daily
- 周统计: GET /api/stats/weekly
- 月统计: GET /api/stats/monthly

【数据目录】
- A股数据库: data/a_stock_db/a_stock.db
- 消息数据库: data/messages.db
- 匹配结果: data/stock_mentions.db
- 日志文件: logs/
- 密钥存储: output/account_keys.json

【注意事项】
- 首次运行需要获取数据库密钥（可能需要重启微信）
- A股数据库已内嵌，首次运行自动释放
- 可通过API手动更新A股数据库
```

---

## 7. 打包验证清单

### 7.1 文件完整性验证

| 验证项 | 验证方法 | 预期结果 |
|--------|----------|----------|
| EXE文件存在 | 检查dist/目录 | 微信群消息监听_v3.0.0.exe 存在 |
| 文件大小合理 | 查看文件属性 | 大于50MB（含三模块+数据） |
| 文件可执行 | 双击运行 | 程序正常启动，显示控制台窗口 |

### 7.2 模块1功能验证

| 验证项 | 操作 | 预期结果 |
|--------|------|----------|
| 进程检测 | 启动微信后运行EXE | 检测到微信进程PID |
| 账号识别 | 自动检测 | 识别当前登录账号 |
| 密钥获取 | 按提示操作 | 成功获取64位密钥 |
| 数据库连接 | 密钥获取后自动连接 | session.db解密成功 |
| 群聊选择 | 输入关键词搜索 | 显示匹配群聊列表 |
| 消息监控 | 选择群聊后开始监控 | 实时显示群消息 |

### 7.3 模块2功能验证

| 验证项 | 操作 | 预期结果 |
|--------|------|----------|
| A股数据库释放 | 首次运行EXE | data/a_stock_db/a_stock.db 自动创建 |
| 数据库可读 | 检查释放的数据库 | 包含股票数据记录 |
| A股数据更新 | POST /api/update-stock-db | 返回更新成功，索引重新加载 |

### 7.4 模块3功能验证

| 验证项 | 操作 | 预期结果 |
|--------|------|----------|
| FastAPI启动 | 检查后台线程 | 服务在8000端口监听 |
| 健康检查 | GET /api/health | 返回 {"status": "ok"} |
| 全量匹配 | POST /api/refresh | 返回匹配结果统计 |
| 增量匹配 | 等待定时触发或手动调用 | 新消息自动匹配 |
| 日统计 | GET /api/stats/daily | 返回当日统计数据 |
| 黑名单过滤 | 检查匹配结果 | 黑名单中的误匹配已过滤 |

### 7.5 运行时数据验证

| 验证项 | 操作 | 预期结果 |
|--------|------|----------|
| 数据目录创建 | 检查EXE同目录 | data/、logs/、output/ 目录已创建 |
| 日志文件写入 | 检查logs/目录 | 生成日志文件 |
| 消息数据库 | 监控运行后检查 | data/messages.db 有新记录 |
| 匹配结果 | 匹配运行后检查 | data/stock_mentions.db 有匹配记录 |

---

## 8. 已知风险与应对

### 8.1 打包风险

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| pandas被排除导致akshare不可用 | A股数据更新失败 | 打包后测试，必要时移除pandas排除 |
| EXE体积过大（>200MB） | 分发不便 | 评估是否切换onedir模式或进一步排除不需要的模块 |
| onefile启动慢（>30秒） | 用户体验差 | 评估是否切换onedir模式 |
| FastAPI在后台线程中运行不稳定 | API服务中断 | 改用子进程方式启动 |
| uvicorn的lifespan在非主线程异常 | 启动失败 | 测试后调整启动方式 |

### 8.2 运行时风险

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| A股数据库释放失败 | 模块3无法匹配 | 降级：首次启动时自动从网络下载数据 |
| 端口8000被占用 | FastAPI启动失败 | 检测端口占用，提示用户或自动换端口 |
| 微信版本更新导致Hook失效 | 密钥获取失败 | 提供手动输入密钥降级方案 |
| DLL缺失 | 程序无法启动 | 提示安装vc_redist.x64.exe |

---

## 9. 实施步骤

### 9.1 打包实施顺序

```
步骤1: 创建 src/main.py（统一入口文件）
  └── 实现路径重定向、A股数据库释放、FastAPI后台启动、微信监控主线程

步骤2: 创建 release_v3.spec（打包配置）
  └── 基于simple_monitor.spec扩展，加入模块2/3的数据文件和隐式导入

步骤3: 创建 build_exe_v3.bat（构建脚本）
  └── 基于build_exe.bat扩展，加入A股数据库检查步骤

步骤4: 执行首次打包测试
  └── 运行 build_exe_v3.bat，检查打包结果

步骤5: 功能验证
  └── 按第7节验证清单逐项测试

步骤6: 问题修复
  └── 根据验证结果修复打包配置或入口文件

步骤7: 更新使用说明.txt
  └── 更新release/使用说明.txt为v3.0.0版本内容

步骤8: 最终发布
  └── 确认所有验证通过，生成最终发布包
```

---

*报告生成时间: 2026-07-25*
