# 模块3 & 模块4 需求规格说明书

**项目名称**：WeChatDataAnalysis - 微信群数据分析与A股股票匹配系统  
**文档版本**：v1.0  
**创建日期**：2026-07-24  
**文档状态**：已确认  

---

## 一、项目背景

项目前两个模块（微信群消息监听 + A股数据库）已完成合并，现需开发新功能模块：将微信群监控数据与A股数据库数据进行比对匹配，提取股票提及信息并进行统计分析，最终通过热力方块图（Treemap）可视化展示。

经讨论确认，将原"模块3"拆分为两个独立模块，分别开发、分别测试、分别合并：

| 模块 | 名称 | 功能定位 | Git分支 | 开发顺序 |
|------|------|---------|---------|---------|
| **模块3** | 数据分析 | 微群消息+A股数据→匹配→统计→API | `feature/module3-data-analysis` | 先开发 |
| **模块4** | 界面展示 | 调用模块3 API→Treemap热力方块看板 | `feature/module4-frontend` | 模块3稳定后开发 |

---

## 二、模块3：数据分析 - 详细需求

### 2.1 功能概述

从微信群消息数据库中提取消息文本，与A股数据库中的股票名称/代码进行智能匹配，按日/周/月维度统计每只股票的提及次数及详情，通过FastAPI REST API接口对外提供数据服务。

### 2.2 数据源

| 数据源 | 位置 | 说明 |
|--------|------|------|
| 微信群消息数据库 | `data/messages.db` | 包含群聊消息内容、发送人、发送时间等 |
| A股股票数据库 | `data/a_stock_db/a_stock.db` | 包含股票代码(code)、股票名称(name)，共5633条记录 |

### 2.3 A股数据过滤规则

从5633条记录中过滤掉非个股数据，仅保留正常交易的个股和ETF：

| 过滤类型 | 过滤规则 | 示例 | 数量 |
|----------|---------|------|------|
| 指数类 | 名称包含"指数" | "沪深300指数"、"上证银行股指数" | ~44条 |
| 退市股 | 名称包含"退市" | "退市银鸽"、"退市金钰" | 若干条 |
| **保留** | 正常交易个股 | "平安银行"、"兆易创新" | ~5500+条 |
| **保留** | ETF | 各类ETF基金 | 若干条 |
| **保留** | ST股 | "*ST美丽"、"ST星源" | 若干条（仍参与匹配） |

### 2.4 匹配逻辑

#### 2.4.1 匹配粒度

**精确匹配 + 常见简称/缩写匹配**

- 全称精确匹配：消息中出现"平安银行"→匹配到000001
- 简称匹配：消息中出现"平安"→匹配到"平安银行"000001
- 代码匹配：消息中出现"000001"→匹配到"平安银行"

#### 2.4.2 简称自动生成引擎

**方案：自动生成 + 置信度过滤**

从A股股票全称中自动生成简称候选，按置信度分级：

**生成规则**：去掉常见后缀生成简称
- 后缀列表：股份、集团、控股、银行、证券、科技、电子、信息、发展、实业、能源、电力、医药、通信、建设、机械、材料、新材等
- 示例：
  - "沙河股份" → 去掉"股份" → 简称"沙河"（置信度：高，2字≥2）
  - "方大集团" → 去掉"集团" → 简称"方大"（置信度：高）
  - "浦发银行" → 去掉"银行" → 简称"浦发"（置信度：高）
  - "中国石化" → 去掉后缀无变化 → 无简称生成（需特殊处理"中国"前缀类）

**置信度过滤规则**：
- 简称长度 ≥ 2个字 → 高置信度，自动采纳
- 简称长度 = 1个字 → 低置信度，不采纳（如"美"从"*ST美丽"生成）
- 特殊处理："中国"前缀类股票（如"中国平安"、"中国石化"），简称可能为"平安"、"石化"等

**输出**：生成简称映射表（JSON配置文件），格式：
```json
{
  "平安": {"code": "000001", "full_name": "平安银行", "confidence": "high"},
  "浦发": {"code": "600000", "full_name": "浦发银行", "confidence": "high"},
  "万科": {"code": "000002", "full_name": "万科A", "confidence": "high"}
}
```

#### 2.4.3 边界判断策略

**方案：字符边界 + 股票上下文关键词 + 黑名单过滤**

**规则1：字符边界判断**

股票名称在消息中出现时，其前后必须满足以下条件之一：
- 位于消息首部或尾部
- 前后为标点符号（，。！？、；：""''【】《》等）
- 前后为空格或换行
- 前后为数字

**规则2：股票上下文关键词**

如果股票名称后面紧跟以下股票相关词，也算有效匹配：
- 后缀词：股份、集团、控股、银行、证券、A股、板块等
- 动作词：涨停、跌停、买入、卖出、加仓、减仓、看多、看空、走势、行情、复盘等
- 修饰词：龙头、白马、蓝筹、妖股等

示例：
- ✅ "我看好平安，近期走势不错" → 匹配（"平安"前后为标点/空格）
- ✅ "平安银行涨停了" → 匹配（"平安"后跟"银行"股票相关词）
- ❌ "海洋办公室开会" → 不匹配（"海洋"后跟"办公室"非股票相关词）
- ❌ "去海洋公园玩" → 不匹配（"海洋"后跟"公园"非股票相关词）

**规则3：黑名单过滤**

维护一个黑名单配置文件，记录容易与股票名称混淆的常见词语组合：
```json
{
  "海洋": ["海洋公园", "海洋办公室", "海洋世界"],
  "平安": ["平安夜", "平安保险"],
  "长城": ["长城汽车", "长城电脑"]
}
```
当消息中股票名称前后组合命中黑名单时，该匹配无效。

#### 2.4.4 误匹配防控

核心原则：宁可漏匹配，不可误匹配。

- "海洋办公室" ≠ "海洋股份"（黑名单+上下文判断）
- "平安夜快乐" ≠ "平安银行"（黑名单判断）
- 简称匹配时必须同时满足边界判断和上下文判断
- 发现误匹配后，通过补充黑名单和调整规则修补

### 2.5 统计维度

#### 2.5.1 日统计

- **统计范围**：仅当天（今日0:00至当前时刻）
- **时间导航**：不可切换，始终显示当天实时数据
- **数据来源**：微信群消息中当天匹配到的股票提及

#### 2.5.2 周统计

- **统计范围**：仅本周（本周一0:00至当前时刻）
- **时间导航**：不可切换，始终显示本周实时数据
- **周定义**：ISO标准，周一为每周第一天，周日为最后一天
- **周标注格式**：`X月Y周 M.D-M.D`

**周计算规则**：
- 月份第1周：从本月1日到第一个周日（可能不满7天）
- 月份中间周：完整的周一至周日
- 月份最后一周：从最后一个周一到月末最后一天（可能不满7天）
- 当前周统计到当天为止

**2026年7月示例**（7月1日为周三）：

| 周次 | 标注 | 日期范围 | 天数 |
|------|------|---------|------|
| 第1周 | 7月1周 7.1-7.5 | 7.1(周三)-7.5(周日) | 5天 |
| 第2周 | 7月2周 7.6-7.12 | 7.6(周一)-7.12(周日) | 7天 |
| 第3周 | 7月3周 7.13-7.19 | 7.13(周一)-7.19(周日) | 7天 |
| 第4周 | 7月4周 7.20-7.26 | 7.20(周一)-7.26(周日) | 7天 |
| 第5周 | 7月5周 7.27-7.31 | 7.27(周一)-7.31(周五) | 5天 |

#### 2.5.3 月统计

- **统计范围**：本月1日至当前时刻
- **时间导航**：可通过月份选择器回查历史月份
- **月份天数**：按自然月实际天数统计（注意2月闰年判断：能被4整除且不能被100整除，或能被400整除的年份2月为29天，否则28天）
- **历史月份**：选择历史月份时，统计范围为该月1日至该月最后一天

### 2.6 统计输出数据结构

#### 2.6.1 汇总数据（第一层）

每只股票的提及次数统计：

```json
{
  "period": "2026-07-24",
  "period_type": "daily",
  "stocks": [
    {
      "code": "000001",
      "name": "平安银行",
      "mention_count": 5,
      "rank": 1
    },
    {
      "code": "000002",
      "name": "万科A",
      "mention_count": 3,
      "rank": 2
    }
  ]
}
```

#### 2.6.2 详情数据（第二层）

点击某只股票后，展示每次提及的详细信息：

```json
{
  "code": "000001",
  "name": "平安银行",
  "period": "2026-07-24",
  "period_type": "daily",
  "mention_count": 5,
  "details": [
    {
      "sender": "张三",
      "timestamp": "2026-07-24 09:15:32",
      "content": "平安银行今天走势不错"
    },
    {
      "sender": "李四",
      "timestamp": "2026-07-24 10:22:18",
      "content": "平安银行涨停了！"
    }
  ]
}
```

### 2.7 API接口设计

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 日统计汇总 | GET | `/api/stats/daily` | 获取当天股票提及统计 |
| 周统计汇总 | GET | `/api/stats/weekly` | 获取本周股票提及统计 |
| 月统计汇总 | GET | `/api/stats/monthly?year=2026&month=7` | 获取指定月份股票提及统计 |
| 股票详情 | GET | `/api/stock/{code}/details?period_type=daily&date=2026-07-24` | 获取指定股票在指定时段的提及详情 |
| 周标注信息 | GET | `/api/week-info?year=2026&month=7` | 获取指定月份的周标注信息 |

### 2.8 项目结构（模块3）

```
src/stock_analysis/
├── __init__.py
├── main.py                    # FastAPI应用入口
├── config/
│   ├── __init__.py
│   ├── suffix_list.json       # 简称生成后缀列表
│   ├── blacklist.json         # 黑名单配置
│   └── context_keywords.json  # 股票上下文关键词
├── models/
│   ├── __init__.py
│   ├── stock.py               # 股票数据模型
│   └── mention.py             # 提及记录数据模型
├── services/
│   ├── __init__.py
│   ├── stock_loader.py        # A股数据加载与过滤
│   ├── alias_generator.py     # 简称自动生成引擎
│   ├── matcher.py             # 消息文本匹配引擎
│   └── statistics.py          # 时间维度统计服务
├── api/
│   ├── __init__.py
│   └── routes.py              # API路由定义
└── tests/
    ├── __init__.py
    ├── test_alias_generator.py
    ├── test_matcher.py
    └── test_statistics.py
```

---

## 三、模块4：界面展示 - 详细需求

### 3.1 功能概述

调用模块3的FastAPI API接口，以Treemap热力方块图的形式展示股票提及统计数据，支持日/周/月三列并排展示，点击方块查看详情。

### 3.2 界面布局

```
┌──────────────────────────────────────────────────────────────────┐
│                      股票监控数据看板                              │
├──────────────────┬──────────────────┬──────────────────────────┤
│    日统计         │    周统计         │  [月份选择器▼]            │
│  (2026-07-24)    │ 7月4周 7.20-7.24 │    月统计                 │
│                  │                  │   (2026年7月)             │
│ ┌──────┐┌───┐┌─┐│ ┌────────┐┌────┐ │ ┌──────────┐┌──┐         │
│ │平安银行││万科││ ││ │兆易创新 ││平安│ │ │ 中国平安  ││  │         │
│ │000001 ││A  ││ ││ │603986  ││银行│ │ │ 601318   ││  │         │
│ │ 5次   ││3次││ ││ │ 12次   ││8次 │ │ │  25次    ││  │         │
│ └──────┘└───┘└─┘│ └────────┘└────┘ │ └──────────┘└──┘         │
│  ■大块=多提及    │  ■颜色深=多提及   │  ■可切换月份查看           │
│  ■红色渐变       │  ■红色渐变       │  ■红色渐变                │
└──────────────────┴──────────────────┴──────────────────────────┘
```

### 3.3 三列布局详细说明

| 列 | 标题 | 数据范围 | 时间导航 |
|----|------|---------|---------|
| 左列 | 日统计 | 当天实时数据 | 不可切换 |
| 中列 | 周统计 | 本周实时数据（标注"X月Y周 M.D-M.D"） | 不可切换 |
| 右列 | 月统计 | 本月实时数据 | 月份选择器可回查历史 |

### 3.4 热力方块图（Treemap）规则

- **方块大小**：提及次数越多，方块面积越大
- **方块颜色**：红色渐变色，提及次数越多颜色越深
- **方块内容**：股票名称 + 股票代码 + 提及次数
- **排列方式**：按提及次数降序，从左上到右下排列

### 3.5 点击交互

- **点击方块**：跳转到新页面，显示该股票的提及详情
- **详情页内容**：
  - 股票名称、股票代码
  - 统计时段信息
  - 提及记录列表：每次提及的发送人、发送时间、消息内容
- **返回按钮**：详情页有返回按钮，回到主看板页面

### 3.6 月份选择器

- 位于月统计列上方
- 默认显示当前月份
- 可选择历史月份查看
- 选择后刷新月统计数据

### 3.7 数据刷新

- **手动刷新**：页面提供刷新按钮
- **自动刷新**：每5分钟自动刷新一次数据
- **刷新范围**：三列数据同时刷新

### 3.8 技术方案

| 层 | 技术 | 说明 |
|----|------|------|
| 后端 | Python FastAPI | 模块3提供API服务 |
| 前端框架 | Vue 3 + Vite | 轻量级前端框架 |
| 图表组件 | ECharts | Treemap热力方块图 |
| UI组件库 | Element Plus | 日期选择器、按钮等 |
| 运行方式 | 本地Web服务 | Python启动FastAPI，自动打开浏览器 |
| 打包方式 | PyInstaller | 打包为exe，双击运行 |

### 3.9 项目结构（模块4）

```
src/stock_frontend/
├── package.json
├── vite.config.js
├── index.html
├── src/
│   ├── main.js
│   ├── App.vue
│   ├── views/
│   │   ├── Dashboard.vue        # 主看板页面（三列Treemap）
│   │   └── StockDetail.vue      # 股票详情页面
│   ├── components/
│   │   ├── TreeMapColumn.vue    # 单列Treemap组件
│   │   └── MonthSelector.vue    # 月份选择器组件
│   └── api/
│       └── stockApi.js          # API调用封装
└── dist/                        # 构建输出目录
```

---

## 四、开发流程与质量管控

### 4.1 模块3开发流程

```
开发 → 交互测试 → 打包测试 → 审批 → 合并主分支
```

#### 阶段1：开发

| 步骤 | 内容 | 产出 |
|------|------|------|
| 1 | 创建 `feature/module3-data-analysis` 分支 | Git分支 |
| 2 | 搭建项目结构（src/stock_analysis/） | 项目骨架 |
| 3 | 实现A股数据加载与过滤（排除指数+退市股） | stock_loader.py |
| 4 | 实现简称自动生成引擎（去后缀+置信度过滤） | alias_generator.py + suffix_list.json |
| 5 | 实现消息文本匹配引擎（字符边界+上下文+黑名单） | matcher.py + blacklist.json + context_keywords.json |
| 6 | 实现时间维度统计（日/周/月，含周标注计算） | statistics.py |
| 7 | 实现FastAPI API接口 | routes.py + main.py |

#### 阶段2：交互测试

| 步骤 | 内容 | 通过标准 |
|------|------|---------|
| 8 | 编写测试脚本验证匹配准确率 | 测试用例全部通过 |
| 9 | 用真实微信群消息数据测试 | 匹配结果人工审核无误 |
| 10 | 调试优化匹配逻辑 | 误匹配率<5%，漏匹配可接受 |
| 11 | 交互测试通过 | 测试报告输出 |

#### 阶段3：打包测试

| 步骤 | 内容 | 通过标准 |
|------|------|---------|
| 12 | PyInstaller打包为exe | 打包成功，无报错 |
| 13 | 在干净环境测试exe运行 | API服务正常启动，接口响应正常 |
| 14 | 打包测试通过 | 打包测试报告输出 |

#### 阶段4：审批合并

| 步骤 | 内容 | 产出 |
|------|------|------|
| 15 | 提交测试报告给用户审批 | 测试报告文档 |
| 16 | 用户同意后合并到主分支 | 合并完成，分支可删除 |

### 4.2 模块4开发流程（模块3合并后开始）

```
开发 → 交互测试 → 打包测试 → 审批 → 合并主分支
```

#### 阶段1：开发

| 步骤 | 内容 | 产出 |
|------|------|------|
| 1 | 创建 `feature/module4-frontend` 分支 | Git分支 |
| 2 | 搭建Vue 3 + Vite前端项目 | 项目骨架 |
| 3 | 实现三列Treemap热力方块看板 | Dashboard.vue + TreeMapColumn.vue |
| 4 | 实现点击方块→详情新页面 | StockDetail.vue |
| 5 | 实现月份选择器回查 | MonthSelector.vue |
| 6 | 实现数据刷新（手动+5分钟自动） | 刷新逻辑 |
| 7 | 对接模块3 API | stockApi.js |

#### 阶段2：交互测试

| 步骤 | 内容 | 通过标准 |
|------|------|---------|
| 8 | 界面功能测试 | 三列数据正确显示，方块大小/颜色正确 |
| 9 | 交互测试 | 点击方块跳转详情正确，返回正常 |
| 10 | 月份回查测试 | 历史月份数据正确加载 |
| 11 | 刷新测试 | 手动刷新和自动刷新均正常 |

#### 阶段3：打包测试

| 步骤 | 内容 | 通过标准 |
|------|------|---------|
| 12 | 前端构建打包 | npm run build 成功 |
| 13 | PyInstaller整体打包 | exe打包成功 |
| 14 | 干净环境测试 | 双击exe→自动启动服务→自动打开浏览器→界面正常 |

#### 阶段4：审批合并

| 步骤 | 内容 | 产出 |
|------|------|------|
| 15 | 提交测试报告给用户审批 | 测试报告文档 |
| 16 | 用户同意后合并到主分支 | 合并完成 |

---

## 五、关键决策记录

| 序号 | 决策项 | 选择方案 | 决策依据 |
|------|--------|---------|---------|
| 1 | 匹配粒度 | 精确匹配+简称/缩写匹配 | 平衡准确率和召回率 |
| 2 | 简称映射维护 | 自动生成+置信度过滤 | 减少人工维护，高置信度自动采纳 |
| 3 | 边界判断 | 字符边界+上下文关键词+黑名单 | 三重防护，最大限度避免误匹配 |
| 4 | 数据过滤范围 | 排除指数+退市股 | 仅匹配正常交易个股和ETF |
| 5 | 周定义 | 周一至周日（ISO标准） | 中国常用标准 |
| 6 | 月统计边界 | 自然月 | 符合用户习惯 |
| 7 | 时间导航 | 日/周固定，月可回查 | 日/周关注实时，月关注趋势 |
| 8 | 界面展示形式 | Treemap热力方块图 | 直观展示提及热度和频率 |
| 9 | 详情展示方式 | 新页面 | 展示空间充足 |
| 10 | 数据刷新 | 手动+5分钟自动 | 兼顾实时性和性能 |
| 11 | UI技术方案 | 本地Web服务（FastAPI+Vue3+ECharts） | 开发高效，Treemap组件成熟 |
| 12 | 打包方案 | PyInstaller打包exe | 与现有项目一致，用户使用简单 |
| 13 | 模块拆分 | 模块3(数据分析)+模块4(界面展示) | 独立开发、独立测试、更好维护 |
| 14 | Git分支策略 | Feature分支，完成后合并 | 开发期不影响主分支，完成后合并 |

---

## 六、风险与注意事项

1. **匹配准确率风险**：简称匹配可能产生误匹配，需持续优化黑名单和上下文关键词
2. **性能风险**：5633只股票对大量消息进行匹配，需考虑匹配算法效率
3. **数据一致性**：模块3和模块4通过API对接，需确保API接口稳定
4. **打包体积**：FastAPI+Vue3+ECharts打包后exe体积可能较大（80-120MB）
5. **浏览器依赖**：本地Web服务方案依赖用户电脑有浏览器

---

*文档结束*
