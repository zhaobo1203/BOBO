# 构建与打包过程报告 v1.0

## 文档信息

| 项目 | 内容 |
|------|------|
| 项目名称 | 微信群消息监听系统 |
| 版本号 | 1.18.0 |
| 报告日期 | 2026-07-22 |
| 报告版本 | v1.0 |
| 报告类型 | 构建与打包过程 |

---

## 1. 构建环境

### 1.1 构建机器信息

| 配置项 | 值 |
|--------|-----|
| 操作系统 | Windows 10 64-bit |
| Python 版本 | 3.14.6 |
| PyInstaller 版本 | 6.21.0 |
| 构建时间 | 2026-07-22 19:00 |

### 1.2 构建工具版本

| 工具 | 版本 | 用途 |
|------|------|------|
| Python | 3.14.6 | 运行环境 |
| PyInstaller | 6.21.0 | 可执行文件打包 |
| pip | 最新版 | 依赖管理 |

---

## 2. 打包配置

### 2.1 打包模式

| 配置项 | 值 | 说明 |
|--------|-----|------|
| 打包模式 | onefile | 单文件打包 |
| 入口文件 | `src/simple_monitor.py` | 主程序入口 |
| 输出名称 | 微信群消息监听.exe | 可执行文件名 |
| 控制台模式 | console=True | 显示控制台窗口 |
| UPX压缩 | False | 禁用，保证兼容性 |
| 管理员权限 | uac_admin=False | 不强制管理员权限 |

### 2.2 打包配置文件

配置文件: `simple_monitor.spec`

```python
# 关键配置
a = Analysis(
    ['src/simple_monitor.py'],  # 入口文件
    pathex=[PROJECT_ROOT, os.path.join(PROJECT_ROOT, 'src')],
    binaries=[],
    datas=filtered_datas,  # Native DLL 文件
    hiddenimports=hiddenimports,  # 隐式导入模块
    excludes=[...],  # 排除不需要的库
)

exe = EXE(
    ...
    name='微信群消息监听',
    console=True,
    upx=False,
)
```

---

## 3. 数据文件收集

### 3.1 Native DLL 文件

以下 DLL 文件被打包到可执行文件中：

| 文件 | 用途 |
|------|------|
| wcdb_api.dll | WCDB 数据库接口 |
| WCDB.dll | 微信数据库引擎 |
| VoipEngine.dll | 语音通话引擎 |
| wce_integrity*.pyd | 完整性校验模块 |

### 3.2 资源文件

| 文件 | 用途 |
|------|------|
| weflow_wasm_keystream.js | WASM 密钥流处理 |
| wasm_video_decode.js | 视频解码 JS |
| wasm_video_decode.wasm | 视频解码 WASM |

---

## 4. 隐式导入模块

### 4.1 标准库模块

```
sqlite3, winreg, ctypes, ctypes.wintypes
multiprocessing, threading, queue, socket
hashlib, hmac, struct, json, re, logging
datetime, pathlib, tempfile, shutil, typing
dataclasses, collections, functools, itertools
```

### 4.2 第三方库模块

| 模块 | 用途 |
|------|------|
| psutil | 进程管理 |
| pymem | 内存读取 |
| Crypto (pycryptodome) | AES 解密 |
| cryptography | 加密操作 |
| zstandard | zstd 压缩解压 |
| wx_key | 微信密钥提取 |

### 4.3 内部模块

打包的内部模块包括：

- `wechat_decrypt_tool` 核心包
- `wechat_decrypt_tool.routers` 路由模块
- `wechat_decrypt_tool.mcp` MCP 协议模块
- `wechat_decrypt_tool.wrapped` 封装服务

---

## 5. 排除模块

### 5.1 排除的大型库

以下库被排除以减小包体积：

| 模块 | 原因 |
|------|------|
| tkinter | GUI 库，不需要 |
| matplotlib | 绑图库，不需要 |
| numpy | 数值计算，不需要 |
| pandas | 数据分析，不需要 |
| PIL | 图像处理，不需要 |
| cv2 | 计算机视觉，不需要 |
| torch | 机器学习，不需要 |
| tensorflow | 机器学习，不需要 |
| scipy | 科学计算，不需要 |
| IPython | 交互式 Python，不需要 |
| jupyter | Jupyter Notebook，不需要 |
| pytest | 测试框架，不需要 |
| sphinx | 文档生成，不需要 |

---

## 6. 构建过程

### 6.1 构建步骤

```bash
# 步骤 1: 安装依赖
pip install -e .

# 步骤 2: 执行打包
python -m PyInstaller simple_monitor.spec --noconfirm

# 步骤 3: 复制文件到 release 目录
copy /Y "dist\微信群消息监听.exe" "release\"
```

### 6.2 构建输出

| 阶段 | 输出 |
|------|------|
| Analysis | 模块依赖分析完成 |
| PYZ | Python 字节码打包完成 |
| PKG | 归档文件创建完成 |
| EXE | 可执行文件生成完成 |

### 6.3 构建日志

```
55987 INFO: Build complete! 
The results are available in: e:\code2\WeChatDataAnalysis\dist
```

---

## 7. 构建产物

### 7.1 输出文件

| 文件 | 路径 | 说明 |
|------|------|------|
| 微信群消息监听.exe | `dist/微信群消息监听.exe` | 构建输出 |
| 微信群消息监听.exe | `release/微信群消息监听.exe` | 发布包 |

### 7.2 构建中间文件

| 目录 | 说明 |
|------|------|
| `build/simple_monitor/` | 构建中间文件 |
| `build/simple_monitor/warn-simple_monitor.txt` | 构建警告 |
| `build/simple_monitor/xref-simple_monitor.html` | 交叉引用 |

---

## 8. 构建验证

### 8.1 文件完整性

| 验证项 | 状态 |
|--------|------|
| 可执行文件存在 | 通过 |
| 文件大小正常 | 通过 |
| 文件可执行 | 通过 |

### 8.2 功能验证

| 验证项 | 状态 |
|--------|------|
| 进程检测 | 通过 |
| 账号识别 | 通过 |
| 密钥获取 | 通过 |
| 数据库连接 | 通过 |
| 群聊选择 | 通过 |
| 消息监控 | 通过 |

---

## 9. 构建问题记录

### 9.1 已知警告

| 警告 | 说明 | 处理 |
|------|------|------|
| Hidden import 'multiprocessing.freeze_support' not found | PyInstaller 6.x 不需要 | 忽略 |
| Hidden import 'cryptography.hazmat.backends.default_backend' not found | 新版 cryptography 已移除 | 忽略 |
| pycparser.lextab/yacctab not found | 动态生成，不影响功能 | 忽略 |

### 9.2 jieba 语法警告

```
SyntaxWarning: "\." is an invalid escape sequence
```

这是第三方库 jieba 的警告，不影响打包结果。

---

## 10. 发布清单

### 10.1 发布包内容

| 文件 | 用途 |
|------|------|
| 微信群消息监听.exe | 主程序 |
| vc_redist.x64.exe | VC++ 运行时安装包 |
| 使用说明.txt | 用户操作指南 |

---

*报告生成时间: 2026-07-22 19:19*